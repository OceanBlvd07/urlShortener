<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log in</title>
    <link rel="stylesheet" href="/css/main.css" />
</head>
<body class="bg-gray-100 min-h-screen flex flex-col">
    <div class="navbar bg-base-100 shadow-sm">
        <div class="flex-1">
            <a class="btn btn-ghost text-xl" href="/">URL Shortener</a>
        </div>
        <div class="flex-none">
            <ul class="menu menu-horizontal px-1">
                <li><a class="btn btn-md btn-primary" href="/">Home Page</a></li>
            </ul>
        </div>
    </div>
    <div class="flex-1 flex items-center justify-center">
        <div class="w-full max-w-md p-6 bg-white rounded-2xl shadow-lg">
            <h1 class="text-3xl font-bold text-center mb-6">Log in</h1>
            <form action="/login" method="POST" class="flex gap-2 mb-4">
                <input class="input validator input-bordered flex-1" type="email" required placeholder="mail@site.com" name="txtEmail"/>
                <button class="btn btn-primary">Log In</button>
            </form>
        </div>
    </div>
</body>
</html>